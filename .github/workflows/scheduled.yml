---
name: scheduled

on:
  schedule:
    - cron: '11 4 * * *'
jobs:
  update:
      uses: ./.github/workflows/main.yml
      with:
        user: "pi"
        host: "raspberrypi"
        playbook: "update"
      secrets: inherit
      
