---
name: scheduled

on:
  schedule:
    - cron: '11 4 * * *'
jobs:
  update:
      uses: ./.github/workflows/main.yml
      with:
        user: "pi"
        host: "raspberrypi"
        playbook: "update"
      secrets: inherit
  update2:
      uses: ./.github/workflows/main.yml
      with:
        user: "pi"
        host: "pizero2w"
        playbook: "update"
      secrets: inherit
